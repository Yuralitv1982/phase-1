
### ⚔️ [[eng-moc|ENG]] (1ч)
eng-theory:: 0
eng-practice:: 0
- [x] **теория** (15м)
- [ ] **практика** (45м)

### ⚔️ [[h-c-moc|H&C]] (1ч)
h&c-theory:: 0
h&c-practice:: 0
- [ ] **теория** (15м)
- [x] **практика** (45м)

### ⚔️ [[thk-moc|THK]] (1ч)
thk-theory:: 0
thk-practice:: 0
- [ ] **теория** (15м)
- [ ] **практика** (45м)

### ⚔️ [[arc-moc|ARC]] (1ч)
arc-theory:: 0
arc-practice:: 0
- [ ] **теория** (15м)
- [ ] **практика** (45м)



# 🕹 daily quest | 20.01.2026

## 🌍 глобальный таймер смены
```simple-time-tracker
{"entries":[{"name":"Segment 1","startTime":"2026-01-20T18:37:07.491Z","endTime":"2026-01-20T20:42:20.802Z"}]}
```


## 📈 Метрики смены

```dataviewjs
// 1. Получаем текущую страницу
const p = dv.current();

// 2. Собираем все поля, которые заканчиваются на -theory и -practice
const theoryFields = Object.keys(p).filter(k => k.endsWith("-theory"));
const practiceFields = Object.keys(p).filter(k => k.endsWith("-practice"));

// 3. Считаем суммы
const totalTheory = theoryFields.reduce((sum, k) => sum + (Number(p[k]) || 0), 0);
const totalPractice = practiceFields.reduce((sum, k) => sum + (Number(p[k]) || 0), 0);
const effectiveTime = totalTheory + totalPractice;

// 4. Глобальные данные (если есть таймер смены)
const globalDuration = p["global-duration"] || 0;
const wasteTime = Math.max(0, globalDuration - effectiveTime);

// 5. Вывод таблицы метрик
dv.table(["Метрика", "Значение (мин)"], [
    ["Теория (всего)", totalTheory],
    ["Практика (всего)", totalPractice],
    ["Effective Time", `**${effectiveTime}**`],
    ["Waste Time", wasteTime > 0 ? `🔴 ${wasteTime}` : `✅ 0`]
]);
```
## 🐲 BOSS: BACKLOG (Приоритет 100%)
> Не закрыл вчера — умри сегодня.

```dataview
task
where !completed 
where file.name < this.file.name
sort file.name asc
```



```dataviewjs
const current = dv.current();
// Считаем эффективное время прямо здесь для точности
const fields = Object.keys(current).filter(k => k.endsWith("-theory") || k.endsWith("-practice"));
const actual = fields.reduce((sum, k) => sum + (Number(current[k]) || 0), 0);

const plannedHours = current["planned-total-hours"] || 10; 
const plannedMin = plannedHours * 60;
const delta = plannedMin - actual;

dv.header(2, "⚖️ Вердикт системы");

if (delta <= 0) {
    dv.paragraph("✅ **План выполнен.** Красава, Архитектор.");
} else {
    const h = Math.floor(delta / 60);
    const m = delta % 60;
    dv.paragraph(`🔴 **НЕДОКОЛ:** Ты задолжал **${h}ч ${m}м**.`);
    dv.paragraph(`> [ ] 💸 **ДОЛГ:** Отработать за ${current.file.name} #debt`);
}

```

