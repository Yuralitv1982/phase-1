# 🏛 System Architecture: Obsidian Forge

## 🧩 Компоненты логики

### 1. Templater (Генератор)
Используется как основной движок развертывания ежедневных рабочих пространств на основе шаблона `templates/daily-quest.md`.

* **Функция**: 
    * Динамическое определение дня недели через объект `moment()`.
    * Парсинг таблицы из `general/cycle.md` для извлечения актуальных задач.
* **Выходные данные**: 
    * Автозаполнение YAML-свойств (Frontmatter) заметки.
    * Генерация списка активных дисциплин с интегрированными блоками `time-tracker`.

### 2. Dataview / DataviewJS (Анализатор)
Служит вычислительным ядром для мониторинга состояния системы в реальном времени.

* **Backlog Engine**: Выполняет поиск по всему вольту незавершенных задач (`task where !completed`) из прошлых файлов.
* **Verdict Logic**: Сравнивает метаданные текущего сеанса (`effective-time`) с плановыми показателями из YAML. Вычисляет дефицит времени и автоматически маркирует его тегом `#debt`.

### 3. Tracker (Визуализатор)
Отвечает за графическое представление накопленных данных и KPI.

* **Heatmap**: Календарь интенсивности, отображающий плотность работы на основе `effective-time`.
* **Bullet Graph / Line Chart**: Динамика выполнения плана и соотношение «Работа / Прокрастинация» (`waste-time`).

---

## 💾 Синхронизация и Безопасность

* **Инструмент**: Плагин **Obsidian Git**.
* **Транспорт**: Протокол SSH (ключ `id_ed25519`) для исключения ввода паролей.
* **Режим работы**:
    * **Auto-commit**: Каждые 60 минут при наличии изменений.
    * **Final Push**: Автоматическая отправка данных в ветку `main` при закрытии Obsidian.
* **Контроль**: GitHub выступает в роли удаленного «зеркала» и истории Proof of Work.

---

## 🛠 Окружение (Environment)
* **OS**: Fedora 43 LTS (KDE Plasma)
* **Shell**: zsh
* **IDE Integration**: WebStorm (для сложного кода), Obsidian (для архитектуры и логов)