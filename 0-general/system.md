## 🛠 1. README.md для GitHub (Архитектура Системы)

Этот файл должен быть лицом репозитория `phase-1`. Он объясняет, как твои ежедневные «квесты» превращаются в квалификацию Software Architect.

### **Phase-1: The Software Architect’s Forge**

#### 1. **Концепция: Система «Мануфактура»**

Философия управления обучением, основанная на принципе **Proof of Work**. Каждое знание должно быть зафиксировано, каждый час — обоснован, каждый провал — учтен как долг.

#### **Стек управления**

- **OS**: Fedora 43 LTS (KDE Plasma).
- **Core**: Obsidian + Git.
- **Logic**: Templater + DataviewJS + Tracker.
- **Terminal**: zsh + gh CLI.

#### **Ключевые принципы**

- **Single-threaded Focus**: Одна задача в фокусе, остальное — в бэклоге.
    
- **Immutable Snapshots**: Каждый день — это отдельный снимок (`dayly/YYYY-MM-DD.md`), фиксирующий дельту прогресса.
    
- **Правило 60 минут**: Минимальный квант времени на дисциплину.
    
- **Git как Пульс**: Нет коммита — нет работы.
    

---

## 📂 2. Структура хранения: Код vs Заметки

Для Software Architect критически важно разделять **артефакты знаний** (Obsidian) и **экспериментальный код** (Node.js проекты).

### **Стратегия «Лаборатория и Субстрат»**

В Obsidian репозитории (`phase-1`) мы храним только **описание** и **ссылки**, а сам код живет в параллельной структуре.

|Директория|Тип данных|Описание|
|---|---|---|
|`~/develop/projects/`|**Лаборатория**|Реальные Node.js проекты, репозитории сервисов.|
|`~/develop/temp/`|**Песочница**|Быстрые тесты, скрипты на один раз.|
|`~/obsidian/phase-1/`|**Субстрат**|Твой вольт: MOC, ежедневные отчеты, теория.|

Експортувати в Таблиці

#### **Связующее звено: `git-memory.md`**

Файл в папке `general/` служит мостом:

- В ежедневном отчете (`daily-quest`) ставится ссылка на коммит из «Лаборатории».
    
- В `git-memory.md` фиксируются пути к актуальным проектам в твоей файловой системе Fedora.
    

---
## ⚙️ 3. Алгоритм автоматизации («Мануфактура»)

Система автоматизирует рутину, позволяя сфокусироваться на коде.

- **Динамическое планирование**: Скрипт парсит `cycle.md` и вытягивает задачи на текущий день недели (D01-D07).
    
- **Распределение**: Нагрузка делится на 25% теории и 75% практики.
    
- **Управление долгами**: Блок `BOSS: BACKLOG` автоматически находит все незакрытые задачи из прошлых файлов.
    
- **Вердикт**: Система сравнивает `planned-total-hours` с `effective-time` и фиксирует временной долг (#debt).
## 🧠 3. Описание системы (Obsidian Logic)

Твоя система построена на трех китах автоматизации, заложенных в шаблоне `daily-quest.md`:

### **I. Динамическое планирование (The Cycle)**

Система автоматически парсит файл `cycle.md`, извлекая задачи на текущий день недели (D01-D07).

- **25% Теория**: Автогенерация задач на чтение и конспектирование.
    
- **75% Практика**: Формирование рабочих блоков для кодинга с таймерами.
    

### **II. Долговая яма (Backlog Management)**

Блок `BOSS: BACKLOG` в Obsidian использует Dataview для поиска всех незавершенных задач из прошлых файлов.

- **Фиксация недокола**: Если `effective-time` меньше плана, скрипт автоматически генерирует задачу `#debt`.
    
- **Приоритет**: Долги всегда отображаются выше текущих задач.
    

### **III. Вердикт и Метрики**

В конце каждой смены фиксируются метрики:

- `global-duration`: Общее время сессии.
    
- `effective-time`: Чистое время работы над дисциплинами.
    
- `waste-time`: Потерянное время. Это позволяет строить графы Tracker и видеть реальную цену обучения.