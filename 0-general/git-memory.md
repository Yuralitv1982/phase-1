
Мы будем использовать стандарт **Conventional Commits**, адаптированный под твою «Мануфактуру».

---

## 🛠 Структура коммита

Plaintext

```
<type>(<scope>): <subject>
```

- **type**: Что именно ты сделал (теория, практика, конфиг).
- **scope**: Какая дисциплина затронута (js, eng, utl, hc).
- **subject**: Краткое описание на английском (или русском, если пока тяжело, но лучше привыкать к English).

---

## 📋 Типы коммитов (The List)

| Тип        | Когда использовать                                             |
| ---------- | -------------------------------------------------------------- |
| **lab**    | Практический код, упражнения, "большие поделки".               |
| **theory** | Конспекты, резюме прочитанных глав, архитектурные схемы.       |
| **drill**  | Простые упражнения на автоматизм "дрочка"                      |
| **infra**  | Настройка Fedora, скрипты `bash`, конфиги Obsidian/Git.        |
| **report** | Синхронизация ежедневных квестов (`daily-quest`).              |
| **feat**   | Новая фича в твоем проекте (например, в конструкторе сайтов).  |
| **fix**    | Исправление ошибки в коде или опечатки в документации.         |
| **chore**  | Рутина: переименование файлов, чистка структуры, `.gitignore`. |

Експортувати в Таблиці

---

## 🚀 Примеры для твоей системы

### Для учебы и теории:

- `theory(js): summarize event loop and microtasks`
- `theory(arc): analyze cpu cache levels for performance`
- `theory(eng): add 10 new patterns for behavioral interview`

### Для практики и кода:

- `lab(js): implement custom promise from scratch`
- `lab(hc): solve centering issue with subgrid`  
- `feat(prj): add router to website constructor`


### Для системы (Obsidian/Linux):

- `infra(utl): update daily-quest template with moment logic`
- `infra(utl): create sync script for git automation`
- `report(mgt): sync metrics for 2026-01-20 and close backlog`


---

## 📜 Золотые правила

1. **Imperative mood**: Пиши «add», а не «added». Как будто отдаешь команду системе.
2. **No period**: В конце первой строки точка не ставится.
3. **Kebab-case**: Внутри `scope` всегда используй маленькие буквы.
4. **Atomic commits**: Сделал одну задачу (например, закрыл теорию по JS) — закоммитил. Не вали всё в одну кучу в конце дня.

---

#### ключ ssh
Настройка SSH (чтобы не вводить пароль)
Прежде всего, нужно подружиться с GitHub по SSH. Это стандарт для архитектора.
Генерация ключа (если его еще нет): ssh-keygen -t ed25519 -C "your_email@example.com"

Добавление в GitHub:
Скопируй ключ: 
```
cat ~/.ssh/id_ed25519.pub
```
Вставь его в настройках GitHub: Settings -> SSH and GPG keys -> New SSH key.
Проверка: ssh -T git@github.com

Если папка еще не на GitHub, выполни эти команды в терминале внутри папки с проектом
для начала создаем репо на сервере...


```
echo "# phase-1" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:Yuralitv1982/phase-1.git
git push -u origin main
```

